import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const NewsContent = () => {
  return (
    <View>
      <Text>Ini Content Match</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default NewsContent;
